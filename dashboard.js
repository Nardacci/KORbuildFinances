/* KORbuild Finances — Dashboard V5.4 */
(()=>{'use strict';const $=id=>document.getElementById(id),brl=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}),db=()=>KORbuildAuth.client.schema('finances');function setText(id,v){const e=$(id);if(e)e.textContent=v}function showError(m){const e=$('status');if(e){e.textContent=m;e.classList.remove('hidden')}}function deadline(s,y){if(!s||!y)return null;const d=new Date(s+'T12:00:00');d.setFullYear(d.getFullYear()+Number(y));return d}function initials(n){return String(n||'').trim().split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()||'A'}function lang(){return localStorage.getItem('korbuild-language')||'pt-BR'}function setupHeader(u,n){setText('user-name',n);setText('user-email',u.email||'');setText('user-avatar',initials(n));setText('menu-full-name',n);setText('menu-full-email',u.email||'');setText('menu-avatar',initials(n));$('user-menu-btn')?.addEventListener('click',e=>{e.stopPropagation();const m=$('user-menu');m?.classList.toggle('hidden')});$('logout')?.addEventListener('click',async()=>{await KORbuildAuth.logout();location.replace('index.html')});$('language-toggle')?.addEventListener('click',()=>{localStorage.setItem('korbuild-language',lang()==='pt-BR'?'en-US':'pt-BR');location.reload()})}function wireQuickLaunch(){const b=$('quick-launch'),m=$('quick-menu'),mb=$('mobile-launch'),toggle=e=>{e.stopPropagation();m?.classList.toggle('hidden')};b?.addEventListener('click',toggle);mb?.addEventListener('click',toggle)}function wireFlowDrilldown(){const map=[['flowIncome','income'],['flowTransfer','transfer'],['flowInvestment','investment'],['flowExpense','expense']];map.forEach(([id,type])=>{const value=$(id),row=value?.closest('.flow-row');if(!row)return;row.setAttribute('role','link');row.setAttribute('tabindex','0');row.style.cursor='pointer';const go=()=>{location.href=`dashboard-movements.html?type=${type}`};row.addEventListener('click',e=>{if(e.target.closest('a'))return;go()});row.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();go()}})})}async function load(){const s=await KORbuildAuth.session();if(!s?.user){location.replace('index.html');return}const {data:w,error:we}=await db().from('user_workspaces').select('id,display_name,country,primary_currency,setup_completed').eq('user_id',s.user.id).maybeSingle();if(we)throw we;if(!w?.setup_completed){location.replace('workspace.html');return}const now=new Date(),ms=new Date(now.getFullYear(),now.getMonth(),1),nm=new Date(now.getFullYear(),now.getMonth()+1,1),start=ms.toISOString().slice(0,10),end=nm.toISOString().slice(0,10);const [{data:g,error:ge},{data:accounts,error:ae},{data:incomes,error:ie},{data:plans,error:pe},{data:positions,error:pose},{data:investmentTx,error:te},{data:expenses,error:ee},{data:transfers,error:tre}]=await Promise.all([db().from('goals').select('id,name,target_amount,target_years,start_date,initial_wealth,include_initial_wealth,created_at').eq('workspace_id',w.id).order('created_at',{ascending:false}).limit(1),db().from('accounts').select('opening_balance,currency').eq('workspace_id',w.id),db().from('incomes').select('amount,currency,status,receipt_date').eq('workspace_id',w.id),db().from('plans').select('goal_id,projected_monthly_contribution,projected_monthly_rate,created_at').eq('workspace_id',w.id).order('created_at',{ascending:false}).limit(1),db().from('investment_positions').select('investment_id,position_value,cost_basis,result,return_pct,currency,valuation_status').eq('workspace_id',w.id),db().from('investment_transactions').select('transaction_type,amount,currency,transaction_date').eq('workspace_id',w.id).gte('transaction_date',start).lt('transaction_date',end),db().from('expenses').select('amount,status,paid_date,planned_date,currency').eq('workspace_id',w.id),db().from('transfers').select('source_amount,destination_amount,source_currency,destination_currency,transfer_date').eq('workspace_id',w.id).gte('transfer_date',start).lt('transfer_date',end)]);if(ge||ae||ie||pe||pose||te||ee||tre)throw ge||ae||ie||pe||pose||te||ee||tre;const goal=(g||[])[0]||null,plan=(plans||[])[0]||null,ar=accounts||[],ir=incomes||[],pr=positions||[],tx=investmentTx||[],er=expenses||[],tr=transfers||[],primary=w.primary_currency||'BRL',cc=v=>String(v||'').split(/\s+—\s+/)[0].trim().toUpperCase(),primaryCode=cc(primary);const balance=ar.filter(a=>cc(a.currency)===primaryCode).reduce((s,a)=>s+Number(a.opening_balance||0),0);const incomeThisMonth=ir.filter(i=>i.status==='realized'&&i.receipt_date>=start&&i.receipt_date<end&&cc(i.currency)===primaryCode).reduce((s,i)=>s+Number(i.amount||0),0);const investmentValue=pr.filter(a=>cc(a.currency)===primaryCode).reduce((s,a)=>s+Number(a.position_value||0),0);const investedThisMonth=tx.filter(t=>['contribution','adjustment'].includes(t.transaction_type)&&cc(t.currency)===primaryCode).reduce((s,t)=>s+Number(t.amount||0),0);const realizedExpensesThisMonth=er.filter(e=>e.status==='realized'&&String(e.paid_date||'')>=start&&String(e.paid_date||'')<end&&cc(e.currency)===primaryCode).reduce((s,e)=>s+Number(e.amount||0),0);const transferTotal=tr.filter(t=>cc(t.source_currency)===primaryCode).reduce((s,t)=>s+Number(t.source_amount||0),0);const target=Number(goal?.target_amount||0),considered=goal?.include_initial_wealth?Number(goal.initial_wealth||0):balance+investmentValue,progress=target>0?Math.min(100,Math.max(0,considered/target*100)):0,remaining=Math.max(0,target-considered),d=deadline(goal?.start_date,goal?.target_years),en=lang()==='en-US',money=v=>primaryCode==='BRL'?brl(v):Number(v||0).toLocaleString(en?'en-US':'pt-BR',{style:'currency',currency:primaryCode,maximumFractionDigits:0}),surplus=incomeThisMonth-investedThisMonth-realizedExpensesThisMonth;setupHeader(s.user,w.display_name||s.user.email?.split('@')[0]||'André');setText('hello',`${en?'Hello':'Olá'}, ${w.display_name||'você'}`);setText('goalName',goal?.name||'Nenhum sonho configurado');setText('goalTarget',money(target));setText('wealth',money(considered));setText('balance',money(balance));setText('investmentValue',money(investmentValue));setText('investmentNote',pr.length?`${pr.length} ${pr.length===1?'investimento':'investimentos'}`:'Nenhum investimento ainda');setText('wealthNote',goal?.include_initial_wealth?'Patrimônio inicial considerado':'Contas + investimentos');const bar=$('progressBar');if(bar)bar.style.width=progress+'%';setText('progressLabel',progress.toLocaleString(en?'en-US':'pt-BR',{maximumFractionDigits:1})+'%');setText('remaining',money(remaining));setText('deadline',d?d.toLocaleDateString(en?'en-US':'pt-BR',{month:'long',year:'numeric'}):'—');setText('goalStatus',goal?(progress>=100?'Objetivo alcançado':plan?'Você está no caminho':'Objetivo configurado'):'Configure seu sonho');setText('accountCount',`${ar.length} ${ar.length===1?'conta':'contas'}`);setText('flowIncome',money(incomeThisMonth));setText('flowTransfer',money(transferTotal));setText('flowInvestment',money(investedThisMonth));setText('flowExpense',money(realizedExpensesThisMonth));setText('surplus',money(surplus));setText('surplusNote','Receitas − investimentos − despesas realizadas no mês.');setText('evolutionValue',money(considered));setText('evolutionNote','O histórico real será formado pelos fechamentos patrimoniais mensais.');setText('insightTitle',goal?'Seu painel está pronto para acompanhar sua jornada.':'Comece definindo seu sonho.');setText('insightText',goal?'Conforme você registra receitas, investimentos e despesas, o sistema poderá comparar sua evolução com o ritmo necessário para o objetivo.':'Defina seu objetivo financeiro para transformar seus números em uma jornada clara.');setText('monthLabel',now.toLocaleDateString(en?'en-US':'pt-BR',{month:'long',year:'numeric'}));wireQuickLaunch();wireFlowDrilldown()}load().catch(e=>{console.error(e);showError('Não foi possível carregar os dados do seu espaço financeiro.')});})();
