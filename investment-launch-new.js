/* KORbuild Finances — New Investment Launch V1 */
(()=>{'use strict';
const $=id=>document.getElementById(id),db=()=>KORbuildAuth.client.schema('finances');let workspace=null,investments=[];
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const initials=n=>String(n||'').trim().split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()||'A';
function header(u){const n=workspace.display_name||u.user_metadata?.full_name||u.email?.split('@')[0]||'André';['user-name','menu-full-name'].forEach(id=>$(id).textContent=n);['user-email','menu-full-email'].forEach(id=>$(id).textContent=u.email||'');['user-avatar','menu-avatar'].forEach(id=>$(id).textContent=initials(n));$('user-menu-btn').onclick=e=>{e.stopPropagation();$('user-menu').classList.toggle('hidden')};document.addEventListener('click',e=>{if(!document.querySelector('.user-menu-wrap').contains(e.target))$('user-menu').classList.add('hidden')});$('logout').onclick=async()=>{await KORbuildAuth.logout();location.replace('index.html')}}
function msg(t,k='error'){const e=$('form-message');e.textContent=t;e.className='message '+k}
function selected(){return investments.find(i=>i.id===$('investment').value)}
function updateHelp(){const i=selected(),e=$('type-help');e.textContent=i?`Moeda da operação: ${i.currency}. O lançamento será gravado nessa moeda e ficará vinculado a ${i.name}.`:'Selecione um investimento para visualizar a moeda da operação.'}
async function loadInvestments(){const r=await db().from('investments').select('id,name,symbol,currency').eq('workspace_id',workspace.id).order('name',{ascending:true});if(r.error)throw r.error;investments=r.data||[];$('investment').innerHTML='<option value="">Selecione...</option>'+investments.map(i=>`<option value="${i.id}">${esc(i.name)}${i.symbol?' · '+esc(i.symbol):''} — ${esc(i.currency)}</option>`).join('');$('investment').onchange=updateHelp;const params=new URLSearchParams(location.search);if(params.get('investment'))$('investment').value=params.get('investment');updateHelp()}
$('transaction-date').value=new Date().toISOString().slice(0,10);
$('save').onclick=async()=>{const b=$('save');b.disabled=true;msg('');try{const investmentId=$('investment').value,type=$('transaction-type').value,date=$('transaction-date').value,amount=Number($('amount').value),qraw=$('quantity').value.trim(),praw=$('unit-price').value.trim(),quantity=qraw===''?null:Number(qraw),unitPrice=praw===''?null:Number(praw),description=$('description').value.trim(),inv=selected();if(!inv)throw Error('Selecione o investimento.');if(!date)throw Error('Informe a data.');if(!Number.isFinite(amount)||amount<=0)throw Error('Informe um valor válido.');if(quantity!==null&&(!Number.isFinite(quantity)||quantity<=0))throw Error('Informe uma quantidade válida ou deixe o campo em branco.');if(unitPrice!==null&&(!Number.isFinite(unitPrice)||unitPrice<=0))throw Error('Informe um preço unitário válido ou deixe o campo em branco.');const r=await db().from('investment_transactions').insert({workspace_id:workspace.id,investment_id:investmentId,transaction_type:type,transaction_date:date,quantity,unit_price:unitPrice,amount,currency:inv.currency,description:description||null});if(r.error)throw r.error;location.replace('investment-launches.html?created=1')}catch(e){console.error(e);msg(e.message||'Não foi possível salvar o lançamento.')}finally{b.disabled=false}};
async function init(){const s=await KORbuildAuth.session();if(!s?.user){location.replace('index.html');return}const r=await db().from('user_workspaces').select('id,display_name,setup_completed').eq('user_id',s.user.id).maybeSingle();if(r.error)throw r.error;if(!r.data||!r.data.setup_completed){location.replace('workspace.html');return}workspace=r.data;header(s.user);await loadInvestments()}init().catch(e=>{console.error(e);msg('Não foi possível carregar os investimentos.')});
})();